# Chapter 13 Graph Neural Networks

1. Does each node in a graph in we're using for training have to have the same dimensions?
2. In section 13.2 Neural Message-Passing, Why is only the final layer invariant?
3. In section 13.2.5 Node Classification, Can transductive (semi-supervised) learning out perform inductive (purely supervised) learning?
4. In section 13.3.2 Edge Embeddings, from the equations, does it mean that the node embeddings of l+1 depend on the edge embeddings of l+1? Shouldn't edges & node embeddings be completely independent?
5. In section 13.3.4, Over-Smoothing, How/Why do the node embeddings become too similar as they are computed over multiple layers?
6. In section 13.3.6, Geometric Deep Learning, Why is edge embedding used in the update function for coordinate embedding/vector? Ideally, coordinates shouldn't depend on any properties of edges/nodes correct?
